<!--<div class="container">
  <ul class="nav nav-tabs" id="watchlistTabs" role="tablist">
    <li class="nav-item">
      <a class="nav-link active" id="all-tab" data-toggle="tab" href="#all" role="tab" aria-controls="all" aria-selected="true">All</a>
      </li>
    <li class="nav-item">
      <a class="nav-link" id="completed-tab" data-toggle="tab" href="#completed" role="tab" aria-controls="completed" aria-selected="false">Completed</a>
      </li>
    <li class="nav-item">
      <a class="nav-link" id="watching-tab" data-toggle="tab" href="#watching" role="tab" aria-controls="watching" aria-selected="false">Watching</a>
      </li>
  </ul>

  <div class="tab-content" id="watchlistTabsContent">
    <div class="tab-pane fade show active" id="all" role="tabpanel" aria-labelledby="all-tab">
      <% @watchlists.each do |watchlist| %>
          <p><%= watchlist.title %></p>
        <% end %>
      <% end %>
    </div>
    <div class="tab-pane fade" id="completed" role="tabpanel" aria-labelledby="completed-tab">
      <% @watchlists.each do |watchlist| %>
        <% if watchlist.status == 'completed' %>
          <p><%= watchlist.title %></p>
        <% end %>
      <% end %>
    </div>
    <div class="tab-pane fade" id="watching" role="tabpanel" aria-labelledby="watching-tab">
       <% @watchlists.each do |watchlist| %>
        <% if watchlist.status == 'watching' %>
          <p><%= watchlist.title %></p>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

<script>
  $(document).ready(function() {
    $('#watchlistTabs a').on('click', function(e) {
      e.preventDefault();
      $(this).tab('show');
    });
    });
</script>
-->
